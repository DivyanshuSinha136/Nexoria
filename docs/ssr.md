# `nexoria.ssr`

> **Reserved / empty.** This directory exists in the source tree but contains no files.

Server-side rendering is not implemented in this package. It lives in [`nexoria.render`](render.md) (`render_to_html`, `render_document`) and is orchestrated by [`nexoria.core.App`](core.md). Nothing should be imported from `nexoria.ssr`.
